<html>

<head>
    <title></title>
    <meta charset="utf-8" />
    <script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-cookie/1.4.1/jquery.cookie.min.js"></script>
    <script>
    (function LineAuth() {
        var url = window.location.href; //?id=U001&name=jojo
        console.log(url);
        if(url.indexOf('?')!=-1)
        {
            console.log('fuckfuck4')
            var title = "";
            var start = "";
            var end = "";
            var location = "";
            var ary = url.split('?')[1].split('&');
            for(i=0;i<=ary.length-1;i++)
            {
                console.log(ary[i].split('='));
                if(ary[i].split('=')[0] == 'title'){
                    title = ary[i].split('=')[1];
                    $.cookie('title', decodeURIComponent(title), {path: '/'});
                }else if (ary[i].split('=')[0] == 'start'){
                    start = ary[i].split('=')[1];
                    $.cookie('start', decodeURIComponent(start), {path: '/'});
                }else if (ary[i].split('=')[0] == 'end'){
                    end = ary[i].split('=')[1];
                    $.cookie('end', decodeURIComponent(end), {path: '/'});
                }else if (ary[i].split('=')[0] == 'location'){
                    location = ary[i].split('=')[1];
                    $.cookie('location', decodeURIComponent(location), {path: '/'});
                }
            }
            console.log($.cookie('title'));
            console.log($.cookie('start'));
            console.log($.cookie('end'));
            console.log($.cookie('location'));
        }else{
            $.cookie('title',null, {path: '/'});
            $.cookie('start', null, {path: '/'});
            $.cookie('end', null, {path: '/'});
            $.cookie('location', null, {path: '/'});
        }
        var URL = 'https://access.line.me/oauth2/v2.1/authorize?';
        URL += 'response_type=code';
        URL += '&client_id=1608728878';
        URL += '&redirect_uri=http://bn-calendar.herokuapp.com/cookie';
        // URL += '&redirect_uri=http://localhost:8080/html/orderList/cookie.html';
        URL += '&state=none';
        URL += '&scope=profile';
        window.location.href = URL;
    })();
    </script>
</head>

<body>
</body>

</html>