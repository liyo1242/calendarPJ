const stringit = require("../lib");

let obj = {
    type1: String,
    type2: Number,
    type3: Boolean,
    type4: Function,
    type5: Object,
};

const result = stringit(obj);
// console.log(result);
